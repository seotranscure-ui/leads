# This project folder also holds unrelated SEO working files.
# Whitelist approach: ignore everything at the root, then re-include only the
# lead-tracker app and its companions.
/*
!/.gitignore
!/PLAN.md
!/app/
!/prototype/

# within the tracked dirs
node_modules/
dist/
dist-ssr/
*.tsbuildinfo
app/vite.config.js
app/vite.config.d.ts
.env
.env.local
*.local
.DS_Store
Thumbs.db
