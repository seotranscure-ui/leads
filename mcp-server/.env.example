# Copy to .env (local only — never commit). Get these from Supabase → Project Settings → API.
SUPABASE_URL=https://tmpndupgmwkalgrztrgp.supabase.co
# service_role key (SECRET) — needed to read leads past Row-Level Security. Keep it local.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
